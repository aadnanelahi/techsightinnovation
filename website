<!DOCTYPE html>
<html lang="en">
<head>
  <!-- SEO & Meta -->
  <title>TechSight Innovations FZE LLC | Premium IT Solutions UAE</title>
  <meta name="description" content="TechSight Innovations - Premium IT infrastructure, Advanced WiFi, cloud migration, CCTV security across Dubai, Abu Dhabi & UAE. 200+ projects completed." />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  
  <!-- Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Playfair+Display:wght@500;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css"/>
  
  <style>
    :root {
      --navy: #1e3a8a;
      --navy-dark: #111827;
      --navy-light: #3b82f6;
      --gold: #f59e0b;
      --gold-light: #fbbf24;
      --white: #f8fafc;
      --gray-50: #f1f5f9;
      --gray-100: #e2e8f0;
      --glass: rgba(255,255,255,0.05);
      --shadow: 0 25px 50px -12px rgba(0,0,0,0.25);
      --transition: all 0.3s ease;
    }

    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
    body {
      font-family: 'Inter', sans-serif;
      background: linear-gradient(135deg, var(--navy-dark) 0%, var(--navy) 100%);
      color: var(--white);
      line-height: 1.7;
      overflow-x: hidden;
      min-height: 100vh;
    }

    /* ===== HEADER ===== */
    header {
      position: fixed; top:0; left:0; right:0;
      padding: clamp(1rem, 3vw, 1.5rem) clamp(1rem, 5%, 5%);
      backdrop-filter: blur(20px);
      background: rgba(17,24,39,0.95);
      border-bottom: 1px solid rgba(59,130,246,0.2);
      z-index: 1000;
      transition: var(--transition);
    }
    .header-container {
      display: flex; justify-content: space-between; align-items: center;
      max-width: 1400px; margin: 0 auto;
    }
    .logo-container {
      display: flex; align-items: center; gap: clamp(8px, 2vw, 12px);
    }
    .logo-svg {
      width: clamp(36px, 6vw, 48px);
      height: clamp(36px, 6vw, 48px);
      flex-shrink: 0;
    }
    .logo-text {
      font-family: 'Playfair Display', serif;
      font-size: clamp(1.4rem, 4vw, 1.8rem);
      font-weight: 700;
      background: linear-gradient(135deg, var(--white), var(--gold));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      line-height: 1.1;
    }
    .logo-text span {
      font-weight: 400;
      font-size: clamp(1.1rem, 3vw, 1.5rem);
      display: block;
      margin-top: -2px;
    }

    /* Mobile Menu */
    .nav-toggle {
      display: none;
      background: none; border: none; color: var(--white);
      font-size: 1.8rem; cursor: pointer;
    }
    nav {
      display: flex; gap: clamp(1rem, 3vw, 1.5rem);
    }
    nav a {
      color: var(--white); text-decoration: none;
      font-weight: 500; font-size: clamp(0.95rem, 2.5vw, 1rem);
      position: relative; transition: var(--transition);
    }
    nav a::after {
      content: ''; position: absolute; bottom: -6px; left: 0; width: 0;
      height: 2px; background: var(--gold); transition: var(--transition);
    }
    nav a:hover::after { width: 100%; }

    /* ===== HERO ===== */
    .hero {
      min-height: 100vh;
      display: flex; flex-direction: column; justify-content: center;
      padding: clamp(100px, 15vh, 140px) clamp(1rem, 5%, 5%) clamp(60px, 10vh, 80px);
      text-align: center;
      max-width: 1400px; margin: 0 auto;
    }
    .hero h1 {
      font-family: 'Playfair Display', serif;
      font-size: clamp(2.5rem, 8vw, 5.5rem);
      font-weight: 700; margin-bottom: 1.2rem;
      line-height: 1.2;
      background: linear-gradient(135deg, var(--white) 0%, var(--gold) 50%, var(--gold-light) 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }
    .hero-subtitle {
      font-size: clamp(1rem, 3vw, 1.3rem);
      max-width: 800px; margin: 0 auto 2.5rem;
      opacity: 0.9; font-weight: 300;
    }
    .btn-primary {
      display: inline-flex; align-items: center; gap: 0.8rem;
      padding: clamp(14px, 2.5vw, 18px) clamp(24px, 5vw, 40px);
      background: linear-gradient(135deg, var(--gold), var(--gold-light));
      color: var(--navy-dark); font-weight: 600;
      font-size: clamp(1rem, 2.5vw, 1.1rem);
      border-radius: 12px; text-decoration: none;
      box-shadow: var(--shadow); transition: var(--transition);
    }
    .btn-primary:hover {
      transform: translateY(-3px);
      box-shadow: 0 35px 60px -15px rgba(245,158,11,0.4);
    }

    /* ===== SECTIONS ===== */
    section {
      padding: clamp(60px, 12vh, 120px) clamp(1rem, 5%, 5%);
      max-width: 1400px; margin: 0 auto;
    }
    .section-title {
      font-family: 'Playfair Display', serif;
      font-size: clamp(2.2rem, 6vw, 4rem);
      text-align: center; margin-bottom: 3rem;
      font-weight: 700;
      background: linear-gradient(135deg, var(--white), var(--gold));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }

    /* ===== SERVICES GRID ===== */
    .services-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(min(300px, 100%), 1fr));
      gap: clamp(1.5rem, 4vw, 2.5rem);
    }
    .service-card {
      background: rgba(255,255,255,0.08);
      backdrop-filter: blur(20px);
      border: 1px solid rgba(59,130,246,0.2);
      padding: clamp(2rem, 5vw, 3rem) clamp(1.5rem, 4vw, 2.5rem);
      border-radius: 24px;
      text-align: center;
      transition: var(--transition);
      position: relative;
      overflow: hidden;
    }
    .service-card::before {
      content: ''; position: absolute; top: 0; left: 0; right: 0;
      height: 4px; background: linear-gradient(90deg, var(--gold), var(--gold-light));
      transform: scaleX(0); transition: var(--transition);
    }
    .service-card:hover::before { transform: scaleX(1); }
    .service-card:hover {
      transform: translateY(-12px);
      border-color: var(--gold);
      box-shadow: var(--shadow);
    }
    .service-icon {
      width: clamp(70px, 12vw, 90px);
      height: clamp(70px, 12vw, 90px);
      margin: 0 auto 1.5rem;
      background: linear-gradient(135deg, #f59e0b, #fbbf24);
      border-radius: 20px;
      display: flex; align-items: center; justify-content: center;
      font-size: clamp(1.8rem, 4vw, 2.5rem);
      color: var(--navy-dark);
      box-shadow: 0 8px 20px rgba(245,158,11,0.3);
    }
    .service-card h3 {
      font-family: 'Playfair Display', serif;
      font-size: clamp(1.4rem, 4vw, 1.8rem);
      margin-bottom: 0.8rem;
      font-weight: 600;
      color: var(--white);
    }
    .service-card p {
      font-size: clamp(0.9rem, 2.2vw, 1rem);
      opacity: 0.9;
    }

    /* ===== STATS ===== */
    .stats {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(140px, 1fr));
      gap: clamp(1.5rem, 4vw, 3rem);
      text-align: center;
      margin: clamp(3rem, 8vh, 5rem) 0;
    }
    .stat-item h4 {
      font-size: clamp(2.5rem, 7vw, 3.5rem);
      font-weight: 700;
      background: linear-gradient(135deg, var(--gold), var(--white));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }
    .stat-item p {
      font-size: clamp(0.9rem, 2.2vw, 1rem);
      opacity: 0.8;
    }

    /* ===== CONTACT ===== */
    .contact-section {
      background: rgba(255,255,255,0.05);
      border-radius: clamp(20px, 5vw, 32px);
      padding: clamp(2.5rem, 6vw, 4rem);
      backdrop-filter: blur(20px);
      border: 1px solid rgba(59,130,246,0.2);
    }
    .contact-grid {
      display: grid;
      grid-template-columns: 1fr;
      gap: clamp(2rem, 6vw, 4rem);
      margin-bottom: 2rem;
    }
    .contact-info h3 {
      font-family: 'Playfair Display', serif;
      font-size: clamp(1.5rem, 4vw, 1.8rem);
      margin-bottom: 1.5rem;
    }
    .contact-item {
      display: flex; align-items: center; gap: 1rem;
      padding: clamp(1rem, 3vw, 1.5rem);
      background: rgba(255,255,255,0.08);
      border-radius: 16px;
      transition: var(--transition);
    }
    .contact-item:hover { background: rgba(245,158,11,0.1); }
    .contact-item i {
      width: clamp(40px, 8vw, 50px);
      height: clamp(40px, 8vw, 50px);
      background: var(--gold);
      border-radius: 12px;
      display: flex; align-items: center; justify-content: center;
      font-size: clamp(1.1rem, 2.5vw, 1.3rem);
      color: var(--navy-dark);
      flex-shrink: 0;
    }
    .contact-item div {
      font-size: clamp(0.95rem, 2.2vw, 1.1rem);
    }
    .contact-item a {
      color: var(--gold-light);
      text-decoration: none;
      font-weight: 600;
    }

    /* ===== FORM ===== */
    .form-group {
      margin-bottom: clamp(1rem, 3vw, 1.5rem);
    }
    .form-group input,
    .form-group textarea {
      width: 100%;
      padding: clamp(1rem, 3vw, 1.5rem);
      background: rgba(255,255,255,0.1);
      border: 2px solid rgba(255,255,255,0.2);
      border-radius: 16px;
      color: var(--white);
      font-size: clamp(0.95rem, 2.5vw, 1rem);
      transition: var(--transition);
      font-family: inherit;
    }
    .form-group input::placeholder,
    .form-group textarea::placeholder {
      color: rgba(255,255,255,0.6);
    }
    .form-group input:focus,
    .form-group textarea:focus {
      outline: none;
      border-color: var(--gold);
      box-shadow: 0 0 0 4px rgba(245,158,11,0.1);
    }

    /* ===== FOOTER ===== */
    footer {
      background: var(--navy-dark);
      padding: clamp(2.5rem, 6vw, 4rem) clamp(1rem, 5%, 5%) clamp(1.5rem, 4vw, 2rem);
      text-align: center;
      border-top: 1px solid rgba(59,130,246,0.2);
    }
    footer p {
      font-size: clamp(0.9rem, 2.2vw, 1.1rem);
      opacity: 0.8;
    }
    footer p:first-child {
      margin-bottom: 0.8rem;
    }

    /* ===== MOBILE MENU ===== */
    @media (max-width: 768px) {
      .nav-toggle { display: block; }
      nav {
        position: fixed; top: 100%; left: 0; right: 0;
        background: rgba(17,24,39,0.98);
        backdrop-filter: blur(20px);
        flex-direction: column;
        padding: 1.5rem 5%;
        transform: translateY(-100%);
        opacity: 0; visibility: hidden;
        transition: var(--transition);
      }
      nav.active {
        transform: translateY(0);
        opacity: 1; visibility: visible;
      }
      nav a {
        padding: 1rem 0; font-size: 1.1rem;
        border-bottom: 1px solid rgba(255,255,255,0.1);
      }
      .contact-grid { grid-template-columns: 1fr; }
    }

    @media (min-width: 769px) {
      .contact-grid { grid-template-columns: 1fr 1fr; }
    }
  </style>
</head>
<body>

  <!-- Embedded Logo SVG -->
  <svg style="display:none;" xmlns="http://www.w3.org/2000/svg">
    <symbol id="logo-svg" viewBox="0 0 200 200">
      <g fill="none" stroke="#3b82f6" stroke-width="2">
        <circle cx="100" cy="100" r="60" stroke-dasharray="5,5"/>
        <circle cx="60" cy="70" r="8" fill="#3b82f6"/>
        <circle cx="80" cy="60" r="6" fill="#3b82f6"/>
        <circle cx="110" cy="55" r="7" fill="#3b82f6"/>
        <circle cx="140" cy="70" r="9" fill="#3b82f6"/>
        <circle cx="150" cy="100" r="10" fill="#3b82f6"/>
        <circle cx="135" cy="130" r="8" fill="#3b82f6"/>
        <circle cx="100" cy="140" r="12" fill="#3b82f6"/>
        <circle cx="70" cy="130" r="7" fill="#3b82f6"/>
        <circle cx="55" cy="110" r="6" fill="#3b82f6"/>
        <line x1="60" y1="70" x2="80" y2="60"/>
        <line x1="80" y1="60" x2="110" y2="55"/>
        <line x1="110" y1="55" x2="140" y2="70"/>
        <line x1="140" y1="70" x2="150" y2="100"/>
        <line x1="150" y1="100" x2="135" y2="130"/>
        <line x1="135" y1="130" x2="100" y2="140"/>
        <line x1="100" y1="140" x2="70" y2="130"/>
        <line x1="70" y1="130" x2="55" y2="110"/>
        <line x1="55" y1="110" x2="60" y2="70"/>
        <circle cx="165" cy="55" r="4" fill="#3b82f6" opacity="0.6"/>
        <circle cx="170" cy="80" r="3" fill="#3b82f6" opacity="0.5"/>
        <circle cx="160" cy="115" r="3" fill="#3b82f6" opacity="0.4"/>
      </g>
    </symbol>
  </svg>

  <header>
    <div class="header-container">
      <div class="logo-container">
        <svg class="logo-svg" aria-label="TechSight Innovations Logo">
          <use href="#logo-svg"></use>
        </svg>
        <div class="logo-text">
          TechSight
          <span>Innovations</span>
        </div>
      </div>
      <button class="nav-toggle" aria-label="Toggle menu">Menu</button>
      <nav>
        <a href="#services">Services</a>
        <a href="#projects">Projects</a>
        <a href="#contact">Contact</a>
      </nav>
    </div>
  </header>

  <section class="hero">
    <h1>Premium IT Solutions<br><span style="font-weight:400;">for UAE Businesses</span></h1>
    <p class="hero-subtitle">Complete IT infrastructure design, Advanced WiFi, cloud migration, enterprise security & digital transformation. Serving all 7 Emirates with 200+ completed projects.</p>
    <a href="#contact" class="btn-primary">
      <i class="fab fa-whatsapp"></i> Get Quote Now
    </a>
  </section>

  <section id="services">
    <h2 class="section-title">Our Core Services</h2>
    <div class="services-grid">
      <div class="service-card">
        <div class="service-icon"><i class="fas fa-network-wired"></i></div>
        <h3>IT Infrastructure</h3>
        <p>Complete design, implementation & management of servers, racks, desktops, printers & structured cabling</p>
      </div>

      <!-- ADVANCED WIFI CARD - UPDATED -->
      <div class="service-card">
        <div class="service-icon">
          <svg viewBox="0 0 100 100" style="width:100%; height:100%;">
            <rect x="15" y="15" width="70" height="70" rx="20" fill="#f59e0b"/>
            <path d="M50 35 Q40 45, 30 55 M50 35 Q60 45, 70 55 M50 35 Q45 50, 40 60 M50 35 Q55 50, 60 60" 
                  stroke="#111827" stroke-width="6" fill="none" stroke-linecap="round"/>
            <circle cx="50" cy="70" r="8" fill="#111827"/>
          </svg>
        </div>
        <h3>Advanced WiFi</h3>
        <p>Enterprise WiFi with Ekahau predictive heatmaps, cloud management & 100% coverage</p>
      </div>

      <div class="service-card">
        <div class="service-icon"><i class="fas fa-shield-alt"></i></div>
        <h3>Security Systems</h3>
        <p>Fortinet firewalls, biometric access control, AI CCTV surveillance & 24/7 monitoring</p>
      </div>
      <div class="service-card">
        <div class="service-icon"><i class="fas fa-cloud"></i></div>
        <h3>Cloud Migration</h3>
        <p>Seamless AWS, Azure, Google Cloud migration with zero downtime & full data security</p>
      </div>
      <div class="service-card">
        <div class="service-icon"><i class="fas fa-tools"></i></div>
        <h3>AMC Support</h3>
        <p>24/7 proactive maintenance with 2-hour response SLA across all UAE emirates</p>
      </div>
      <div class="service-card">
        <div class="service-icon"><i class="fas fa-laptop-code"></i></div>
        <h3>Digital Solutions</h3>
        <p>Custom websites, e-commerce, logo design & complete brand identity packages</p>
      </div>
    </div>

    <div class="stats">
      <div class="stat-item">
        <h4>200+</h4>
        <p>Projects Completed</p>
      </div>
      <div class="stat-item">
        <h4>7</h4>
        <p>Emirates Served</p>
      </div>
      <div class="stat-item">
        <h4>24/7</h4>
        <p>Support Available</p>
      </div>
    </div>
  </section>

  <section id="contact">
    <div class="contact-section">
      <h2 class="section-title">Get Your Free Quote</h2>
      
      <div class="contact-grid">
        <div class="contact-info">
          <h3>Instant Contact</h3>
          <div class="contact-item">
            <i class="fab fa-whatsapp"></i>
            <div>
              <div style="font-weight:600;">+971 50 952 2649</div>
              <div><a href="https://wa.me/971509522649" target="_blank">Click to WhatsApp</a></div>
            </div>
          </div>
          <div class="contact-item">
            <i class="fas fa-envelope"></i>
            <div>
              <div style="font-weight:600;">info@tecsightinnovations.com</div>
              <div>Response within 2 hours</div>
            </div>
          </div>
        </div>

        <form>
          <div class="form-group">
            <input type="text" placeholder="Your Name *" required>
          </div>
          <div class="form-group">
            <input type="email" placeholder="Email Address *" required>
          </div>
          <div class="form-group">
            <input type="tel" placeholder="Phone Number">
          </div>
          <div class="form-group">
            <textarea rows="5" placeholder="Tell us about your IT requirements..."></textarea>
          </div>
          <button type="submit" class="btn-primary">
            <i class="fas fa-paper-plane"></i> Send Quote Request
          </button>
        </form>
      </div>
    </div>
  </section>

  <footer>
    <p>© 2025 <strong>TechSight Innovations FZE LLC</strong> | Premium IT Solutions Across UAE</p>
    <p>Dubai • Abu Dhabi • Sharjah • All Emirates</p>
  </footer>

  <script>
    // Mobile Menu Toggle
    const navToggle = document.querySelector('.nav-toggle');
    const nav = document.querySelector('nav');
    navToggle.addEventListener('click', () => {
      nav.classList.toggle('active');
      navToggle.innerHTML = nav.classList.contains('active') ? 'Close' : 'Menu';
    });

    // Smooth Scrolling
    document.querySelectorAll('a[href^="#"]').forEach(anchor => {
      anchor.addEventListener('click', function (e) {
        e.preventDefault();
        const target = document.querySelector(this.getAttribute('href'));
        window.scrollTo({
          top: target.offsetTop - 100,
          behavior: 'smooth'
        });
        if (nav.classList.contains('active')) {
          nav.classList.remove('active');
          navToggle.innerHTML = 'Menu';
        }
      });
    });
  </script>
</body>
</html>
